version: '3.8'

services:
  discord-media-bot:
    build: .
    container_name: discord-media-bot
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
